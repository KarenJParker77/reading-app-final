import { user, book, allBooks, futureBooks, finishedBooks } from "../fakeApi";

export const initialState = {
  user,
  // book,
  allBooks,
  // futureBooks,
  // finishedBooks,
  // currentUserId: 0,
  screenMode: 0,
};
